# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# uv / venv (uv.lock is regenerated by `uv sync`/`uv lock`; not tracked)
.venv/
uv.lock

# Local state written by the poller (keep the checked-in one under data/ if you
# choose to commit it from CI; ignore ad-hoc local runs)
/state.json
*.local.json

# Secrets
.env
